---
- name: Scale up
  hosts: all
  gather_facts: no
  strategy: debug
  tasks:
    - name: Wait for SSH on all hosts.
      wait_for_connection:

- name: "Include OpenShift Install playbook"
  hosts: OSEv3

- include: "/usr/share/ansible/openshift-ansible/playbooks/byo/config.yml"
